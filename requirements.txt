numpy>=1.26.0  # Compatible with Python 3.12
streamlit>=1.32.0  # Works with Python 3.12
torch>=2.0.0  # Compatible with Python 3.12
torchvision>=0.15.0  # Compatible with torch
face_alignment>=1.3.5  # Try latest version; may need system-level dependencies
imageio==2.19.3  # Compatible, but verify with Python 3.12
imageio-ffmpeg==0.4.7  # Should work
librosa>=0.9.2  # Use latest compatible version
numba>=0.61.2  # Latest version for Python 3.12
resampy==0.3.1  # Should work
pydub==0.25.1  # Should work
scipy>=1.11.4  # Compatible with Python 3.12 (1.10.1 is not)
kornia>=0.7.0  # Update to latest for Python 3.12 compatibility
tqdm>=4.66.5  # Latest version
pyyaml>=6.0.2  # Latest version
yacs==0.1.8  # Should work
joblib>=1.1.0  # Should work, but use latest
scikit-image>=0.22.0  # Compatible with Python 3.12 (0.19.3 is not)
basicsr>=1.4.2  # Should work, but verify
facexlib>=0.3.0  # Should work, but verify
gradio>=4.44.0  # Latest version for Python 3.12
gfpgan>=1.3.8  # Latest version
dlib-bin>=19.24.2  # Binary distribution for easier installation
av>=12.3.0  # Latest version for Python 3.12
safetensors>=0.4.5  # Latest version
